# Handoff 2026-05-15 → 2026-05-16

## 当前状态

manastone-diag v0.1.0 已完成核心构建并通过两个真实案例验证。可以发布，但有几个收尾项。

## 关键仓库

| 仓库 | 本地路径 | 说明 |
|------|---------|------|
| manastone-diag | `/Users/ZQ/manastone-diag` | 诊断产品主仓库（public） |
| ui-kit | `/Users/ZQ/ui-kit` | 设计系统 + 模板（public） |
| runtime | `/Users/ZQ/runtime` | 运行时（private） |

## 明天优先级

1. **补完 X220028C4Z0079 的第二、三轮诊断**（工厂反馈打断后跳过了）
2. **发布准备**: release notes + git tag v0.1.0
3. **README 完善**: 补充安装后首次使用截图或说明
4. **ui-kit 截图**: 给模板页面截图加到 README

## 已解决的问题

- LogIngestor 漏采摔倒事件和模式切换 → 已修复并推送
- 技能中的 roboonto CLI 死链 → 已修复
- GitHub repo 权限 → 全部整理完毕

## 已知待修

- LogIngestor 的 `_print_summary()` 输出到 stdout 污染 JSON 报告（临时 suppress 了，需彻底改）
- LogIngestor 已知 dataclass defaultdict stats 写入 bug
- MCAP 全量解码太慢（iter_decoded_messages），需改为采样

## 上下文速查

- manastone-diag 入口: `manastone-diag` 命令 → pi 在 repo 目录下启动
- pi 配置: `.pi/settings.json`(quietStartup) + `.pi/SYSTEM.md`(身份) + `.pi/AGENTS.md`(上下文)
- 诊断流程: SYSTEM.md 中的三轮法 → 16 份领域技能(.pi/skills/) + 6 份知识库(knowledge/)
- 工具: tools/log_ingestor.py + tools/mcap_reader.py + tools/fault_library.py + tools/atop_reader.py
- session 名: `manastone-diag`
