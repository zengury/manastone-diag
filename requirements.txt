# Manastone 诊断助手 v2.0 — Python 依赖
# 安装: pip install -r requirements.txt
#
# 注意: Hermes Agent 自带 Python venv，直接 pip install 即可。
#       tkinter 为 Python 标准库，无需额外安装。

pyyaml>=6.0              # YAML 知识库解析
mcap>=1.3                # MCAP ROS2 bag 读取
mcap-ros2-support>=0.5   # ROS2 消息解码
